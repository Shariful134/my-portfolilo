import { RegisterForm } from "@/components/modules/registeter/Register";

const RegisterPage = () => {
  return (
    <div className="px-10 h-[100vh] flex flex-col  items-center justify-center">
      <RegisterForm></RegisterForm>
    </div>
  );
};

export default RegisterPage;
